[package]
name = "pocketjs-gba-bake"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
microts = { path = "../../../engine/crates/microts", features = ["std"] }
pocketjs-core = { path = "../../../engine/core", features = ["std"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
